# データ取扱いルール

## 目的

このリポジトリ（[10_Gitリソース](README.md)）は、**公開し、他のユーザーが同じ環境・同じツールを再現できるようにすること**を目的とする（2026-09-09〜公開）。そのため、実際の同人活動で発生した実データ（売上・経費・個人情報等）を、意図せず含めてしまうことを構造的・運用的に防ぐ。

## この構成にした理由

プロジェクト全体（`94_同人活動経費管理ツール`直下）ではなく、**`10_Gitリソース`フォルダの中だけをGit管理対象とする**構成にしている。これにより、`01_全体構成`・`02_データモデリング`・`03_リソース構築`・`99_参考ドキュメント`等、実プロジェクトの検討過程や実データ参照物を含みうるフォルダは、そもそもGitの管理範囲の外にあり、`git add`しても追跡されることがない（.gitignoreのパターン漏れに依存しない、構造的な防御）。

公開を前提とした`全体構成`・`データモデリング`・`リソース構築`の各ドキュメントは、実プロジェクト側の内容を元にしつつ、個人環境固有の値（プロジェクトID・請求先アカウント名等）や実データへの言及を取り除いたうえで、このフォルダの配下に別途作成する。

## 絶対にコミットしてはいけないもの

- 実際のイベント頒布記録・実際の経費データなど、金額・日付・取引先等が実在するもの
- 実際の領収書・請求書・委託精算書等の画像・PDF（例：実プロジェクト側`99_参考ドキュメント`にあるような実データ）
- Firestore／BigQueryからエクスポートした実データ（バックアップ、CSV、JSON等）
- サービスアカウントキー・APIキー・OAuthトークン・`.env`等の認証情報
- 個人を特定できる情報（本名・住所・連絡先・屋号等）
- 自分のGCPプロジェクトID・請求先アカウントIDなど、環境固有の識別子をそのまま埋め込んだ設定ファイル（公開用ドキュメントでは`your-project-id`のようなプレースホルダーに置き換える）

## 問題ないもの

- スクリプトで機械的に生成した合成テストデータ（例：実プロジェクト側で使っている`build_testdata.py`生成の`テストデータ.xlsx`）。実在の人物・団体・取引と無関係であることが前提
- ダミー値・プレースホルダーを使った設定例
- スキーマ定義・Security Rules・DDL等、値を含まない構造の情報

## 運用ルール

1. **コミット前に必ず`git status`・`git diff --cached`で、追加されるファイルの中身を目視確認する**。「まとめてadd」をしない
2. 実データを扱う作業は、常に`10_Gitリソース`の**外**（Git管理対象外のフォルダ）で行う
3. 新しい種類の機密情報・データファイルが発生した場合は、[.gitignore](.gitignore)にパターンを追記し、このルールにも追記する
4. 判断に迷うファイルは、コミットせず先に確認する
